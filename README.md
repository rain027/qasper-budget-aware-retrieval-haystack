# Budget-Aware Hybrid Retrieval on QASPER

This repository implements a **budget-aware hybrid retriever**
(Dense + BM25) for evidence retrieval on the **QASPER** dataset
using **Haystack**.

## Features
- Dense retrieval with FAISS
- Sparse BM25 retrieval
- Multi-signal scoring
- Token-budget–aware document selection

## Dataset
This project uses the **QASPER** dataset.